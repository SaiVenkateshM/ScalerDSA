package Streams;

import java.util.*;
import java.util.stream.Collectors;

public class twolistinSingleListExample {
    /*public static void main(String[] args) {
        List<String> programIDs = new ArrayList<>(Arrays.asList("EP01", "EP02", "EP03", "EP04"));
        HashSet<String> setoffirstProgramIDs = new HashSet<>();
        setoffirstProgramIDs.add("EP01");
        setoffirstProgramIDs.add("EP02");
        HashSet<String> setofSecondProgramIDs = new HashSet<>();
        setofSecondProgramIDs.add("EP03");
        setofSecondProgramIDs.add("EP04");
        List<String> sportsEvents = new ArrayList<>();
        sportsEvents.add(String.valueOf(setoffirstProgramIDs));
        sportsEvents.add(String.valueOf(setofSecondProgramIDs));

        // List<String> finalanswer = programIDs.stream()
        //         .filter(programId -> (sportsEvents.stream().filter(sportsEvent ->sportsEvent.contains(programId)). transform(programId, sportsEvent)).collect(Collectors.toList())));
    }

    public static String transform(String programId, String sportsEvent) {

    }*/
}